server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        # Penting: Jika Anda pakai Vue Router, baris ini mencegah error 404 saat refresh
        try_files $uri $uri/ /index.html;
    }
}